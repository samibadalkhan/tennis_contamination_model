"""Stage 3 -- two-component epsilon-contamination mixture (the D5 hypothesis).

F = (1-eps)*F_theta + eps*H, fit by EM on block-level rates. eps is recovered
from the aggregate SHAPE of the block-rate distribution -- per-block labels are
not the deliverable and are not confident at any block size. Report it that way.

Intentionally unimplemented: build only after Stage 0.5 passes.
"""
